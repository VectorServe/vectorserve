/* Fundal modern în degrade */
body {
  font-family: Arial, sans-serif;
  background: linear-gradient(to right, #1e3c72, #2a5298);
  color: white;
  margin: 0;
  padding: 0;
  text-align: center;
}

/* Titluri */
h1, h2 {
  margin-top: 40px;
  text-shadow: 1px 1px 4px black;
}

/* Butoane stilizate */
button {
  background-color: #ffcc00;
  border: none;
  padding: 12px 24px;
  margin: 20px;
  font-size: 18px;
  border-radius: 8px;
  cursor: pointer;
  transition: 0.3s;
}

button:hover {
  background-color: #ffaa00;
  transform: scale(1.05);
}

/* Cronometru și progres */
#timer, #progress {
  font-size: 22px;
  margin: 20px 0;
  padding: 10px;
  background: rgba(0,0,0,0.3);
  border-radius: 10px;
  display: inline-block;
}
